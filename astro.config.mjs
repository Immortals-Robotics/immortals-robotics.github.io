// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://immortals-robotics.github.io',
  // Set this to empty string if your site is at the root of the domain
  base: '/',
});
